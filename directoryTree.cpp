#include <iostream>
#include <algorithm>
#include <fstream> 
#include <sstream>
#include <string>
#include <random>
#include <cstring>

struct SST_node
{
	SST* sst;
	int split;
	SST_node* left;
	SST_node* right;
	SST_node(SST* tab): sst(tab), left(nullptr), right(nullptr), split(0) {}	
};


public class directory
{
	SST_node* root;
	void insert(kv_pair* kv)
	{
		SST_node* curr = 
	}
};